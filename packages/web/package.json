{
  "name": "@opentry/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenTry frontend. Deliberately build-free: plain HTML/CSS/JS served straight by the API, so deployment has no bundler step to fail."
}
